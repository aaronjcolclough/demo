import {
  Component,
  OnInit
} from '@angular/core';

@Component({
  selector: 'home-route',
  templateUrl: 'home.route.html'
})
export class HomeRoute implements OnInit {
  constructor(
  ) { }

  ngOnInit() {
  }
}
